const CameraTracker = (() => {
  const controller = {}

  controller.zoomIn = () => {
    console.log('Something was done.')
  }

  controller.zoomOut = () => {
    console.log('Something was done.')
  }

  return controller
})()
